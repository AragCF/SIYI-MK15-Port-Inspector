# Проверки версии 1.0.0

## Уже выполнено

### 1. CRC и точные кадры SIYI

`host-tests/ProtocolSelfTest.java` скомпилирован и запущен в среде подготовки первой версии.

Проверено байт-в-байт:

```text
0x42 4 Hz = 55 66 01 01 00 00 00 42 02 B5 C0
0x42 OFF  = 55 66 01 01 00 00 00 42 00 F7 E0
0x48      = 55 66 01 00 00 00 00 48 89 1D
```

Результат:

```text
ALL PROTOCOL SELF-TESTS PASSED
```

### 2. Парсер ответа `0x48`

Использован официальный пример ответа SIYI. Парсер проверил CRC и декодировал CH5 как `type=5/entity_id=0 → SA`.

### 3. Парсер `0x42`

Сформирован корректный по CRC синтетический ответ на 16 каналов; проверено декодирование CH5=1050.

### 4. Статическая проверка Java-исходников

Все Java-файлы приложения дополнительно проверялись `javac` против минимального набора Android-заглушек. Это подтверждает целостность Java-синтаксиса и внутренних вызовов, но не заменяет настоящую Android Gradle-сборку.

## Что ещё требует доказательства

- настоящая Android Gradle-сборка текущего GitHub-состояния;
- установка на реальный MK15;
- фактический VID/PID встроенного USB COM;
- Android USB permission на конкретном пульте;
- реальные ответы прошивки MK15 на `0x48`/`0x42`;
- реальные три положения SA.

Для этого в репозитории есть сценарий Windows-сборки, GitHub Actions и сохранение диагностического отчёта в APK.


## Проверки версии 1.1.0

### 5. ProbeDiffSelfTest

Добавлен независимый чистый Java-тест дифференциального поиска. Он проверяет, что при нескольких последовательных снимках изменяющийся RC-канал получает более высокий рейтинг, чем постоянно растущие транспортные и interrupt-счётчики.

Результат в CI:

```text
ALL PROBE DIFF SELF-TESTS PASSED
```

### 6. Windows PowerShell 5.1

GitHub Actions отдельно выполняет синтаксический разбор всех `tools/*.ps1` именно Windows PowerShell 5.1 до Android-сборки.

### 7. Android Gradle build 1.1.0

Текущий код версии 1.1.0, включая USB, UDP, Bluetooth SPP, raw UART, Android Input, SystemProbe и Switch Finder, успешно собран на Windows runner.

GitHub Actions run: `35731912216`.

Остаётся аппаратная проверка: несколько раундов переключения SA на реальном MK15.


## Проверки версии 1.2.0

### 8. ReportToolsSelfTest

Добавлен отдельный чистый Java-тест ZIP-формирования. Проверяется создание ZIP и наличие ожидаемых вложенных файлов отчёта.

Результат в CI:

```text
ALL REPORT TOOLS SELF-TESTS PASSED
```

### 9. Android Gradle build 1.2.0

Текущая версия 1.2.0, включая ZIP-формирование, сохранение в Download/через системный файловый выбор и HTTP multipart-клиент для thesystem, успешно собрана на Windows runner.

GitHub Actions run: `35735936727`.

Аппаратно ещё нужно проверить:
- фактическое сохранение в Download на конкретном MK15;
- наличие флешки в системном файловом выборе;
- HTTP-приём на `?action=siyi_receive`;
- содержимое ZIP после реального сеанса Switch Finder.


## Проверки версии 1.3.0

### 10. ChannelActivityTrackerSelfTest

Добавлен тест моментальных органов управления. Проверяется сценарий кнопки: канал успевает перейти в активное состояние и вернуться в исходное до следующего снимка. Несмотря на это, `changeCount` и `max/min` сохраняют событие.

Результат:

```text
ALL CHANNEL ACTIVITY SELF-TESTS PASSED
```

### 11. Обновлённый ProbeDiffSelfTest

Рейтинг кандидатов теперь сортируется прежде всего по накопленному весу, а не только по числу раундов. Это не позволяет постоянно растущим IRQ-счётчикам вытеснять редкое, но содержательное событие кнопки/RC/Input.

### 12. Android Gradle build 1.3.0

Версия 1.3.0 с UART0/1/2, пассивным Linux input probe, активным C/D-поиском 20 Гц, фиксацией моментальных RC-событий, отдельными кнопками C/D и полным диффом успешно собрана.

GitHub Actions run: `35772320533` — success.


## Проверки версии 1.3.1

Версия 1.3.1 исправляет критическую неопределённость предыдущего опыта: официальный SIYI SDK UART `/dev/ttyHS0` теперь перед открытием переводится в raw-режим с 115200 бод через доступный `stty/toybox stty`. Результат настройки попадает в диагностический snapshot.

AUTO больше не отправляет SIYI-команды в `ttyHS1/2`; активная передача оставлена только для официального UART0, USB и UDP.

Если после активного поиска не приходит ни одного валидного SIYI-кадра, приложение явно сообщает на экране, что нужно проверить `SIYI TX -> Datalink -> Connection`.

GitHub Actions run: `35789984061` — **success**. Прошли PowerShell syntax check, все host-tests, Android Gradle build и публикация APK.


## Проверки версии 1.3.2

Из реального аппаратного отчёта 1.3.1 извлечён повреждённый UART0-кадр:

```text
75 66 02 20 00 0C 00 68 ... 1D BB
```

Установлено, что это SIYI `0x48`, прошедший через TTY IUCLC:
- `55 -> 75`;
- `48 -> 68`.

После восстановления этих байтов CRC из самого аппаратного кадра точно совпадает: `0xBB1D`.

Этот реальный кадр добавлен в `ProtocolSelfTest`. Тест требует:
- восстановить заголовок и команду;
- пройти исходный аппаратный CRC;
- декодировать фактический mapping MK15;
- подтвердить `CH10=C`, `CH11=D`.

Кроме parser fallback, UART0 теперь явно получает `-iuclc` и приложение сохраняет эффективный `stty -a`.


## Проверки версии 1.3.3

Аппаратный отчёт 1.3.2 подтвердил mapping, но не дал ни одного `0x42` channel frame. В эффективном `stty -a` после открытия Java-stream снова был включён `iuclc`.

1.3.3 меняет порядок: ttyHS0 сначала открывается, затем получает 115200 raw и `-iuclc`; эффективные флаги проверяются уже после открытия.

Активный исследователь сначала отправляет **точную официальную команду 4 Гц** (`freq=2`). Если живых channel frames нет, только тогда пробует 20 Гц.


## Аппаратная улика 1.3.3 → исправление 1.3.4

В отчёте 1.3.3 effective `stty -a` показал положительный `istrip`.

Два реальных повреждённых mapping-кадра дают точное доказательство:

```text
calculated CRC = B5 2D
received CRC   = 35 2D

calculated CRC = 80 4A
received CRC   = 00 4A
```

В обоих случаях входной TTY ровно очистил старший бит. Это соответствует `ISTRIP`.

1.3.4 явно задаёт `-istrip -inpck -ignpar -parmrk` после открытия UART0 и отдельно помечает в журнале CRC-повреждения, полностью объясняемые ISTRIP.

Кроме того, 0x42 теперь тестируется изолированно: mapping до запуска потока, затем только три одинаковые команды 0x42 с sequence=0.
