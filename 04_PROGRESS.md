# Прогресс до конечной цели

**Конечная цель текущей работы:** создать универсальное исследовательское приложение для SIYI MK15, которое достоверно определяет, через какой программный интерфейс и какие коммуникационные каналы доступны физические органы управления пульта — в текущем исследовании прежде всего кнопки **C** и **D**.

Результат исследования должен дать точную, воспроизводимую спецификацию для **отдельного будущего приложения** управления дроном и роботом. Разработка и интеграция этого будущего приложения в текущую задачу **не входят**.

| № | Шаг | Критерий завершения | Статус |
|---:|---|---|---|
| 1 | Изучить MK15 и документированные каналы/транспорты | Источники и протоколы определены | выполнен |
| 2 | Реализовать первоначальный инспектор | APK запускается на MK15 | выполнен |
| 3 | Проверить framing/CRC/0x42/0x48 | Автономные тесты проходят | выполнен |
| 4 | Сделать воспроизводимую Windows/GitHub сборку | CI и локальные runs работают | выполнен |
| 5 | Организовать автономный ZIP-отчёт | Download/флешка/thesystem работают | выполнен |
| 6 | Реализовать универсальный поиск транспортов | USB/UDP/UART/Input исследуются | выполнен |
| 7 | Учесть моментальные C/D | Накопление min/max/changeCount реализовано | выполнен |
| 8 | Провести аппаратные серии C/D | Реальные ZIP получены и разобраны | выполнен |
| 9 | Локализовать рабочий SIYI-транспорт | /dev/ttyHS0 подтверждён реальным RX | выполнен |
| 10 | Доказать фактический mapping C/D | Реальный 0x48 по CRC подтверждает CH10=C, CH11=D | выполнен |
| 11 | Подтвердить живые значения C/D | 0x42 устойчиво подтверждает CH10/CH11: 1050 отпущено, 1950 нажато | выполнен |

**Выполнено: 11 / осталось: 0 / всего: 11 — 100%.**

## Что уже доказано на реальном MK15

- рабочий программный транспорт: SIYI Datalink через **/dev/ttyHS0**;
- скорость SDK UART: **115200 бод**;
- реальный mapping конкретного пульта:
  - **CH10 = C**;
  - **CH11 = D**;
- C и D — моментальные органы управления, поэтому исследователь должен наблюдать поток во время удержания, а не только снимок после отпускания.

## Отчёт 1.3.2

Версия 1.3.2 успешно восстановила реальный mapping через CRC-проверяемый IUCLC fallback, однако живых 0x42 кадров в отчёте ещё нет.

Дополнительно обнаружено, что после открытия Java-потоков эффективный termios снова содержит положительный `iuclc`, несмотря на предварительный `-iuclc`. Поэтому в 1.3.3 конфигурация UART переносится **после открытия** FileInputStream/FileOutputStream и проверяется повторно через `stty -a`.

Для следующего опыта 1.3.3 сначала использует точную документированную команду SIYI 4 Гц; если 0x42 не появляется — пробует 20 Гц.


## Отчёт 1.3.3: обнаружен ISTRIP

Отчёт 1.3.3 дал валидный mapping и ещё два повреждённых mapping-ответа. Эффективный termios содержал `istrip`.

CRC повреждённых кадров доказывает причину математически:
- ожидаемый low CRC `B5` пришёл как `35`;
- ожидаемый low CRC `80` пришёл как `00`.

То есть входной TTY очищал bit7.

Выпущена 1.3.4:
- `-istrip` и остальные бинарные входные флаги выключаются после открытия ttyHS0;
- 0x42 запускается точным sequence=0 кадром;
- mapping запрашивается до потока, а не сразу после;
- во время ожидания RC channel frames никаких дополнительных SDK-команд не отправляется.

Прогресс остаётся **10/11 — 91%** до живого подтверждения CH10/CH11.


## Отчёт 1.3.4: path-based stty признан непригодным

На реальном MK15 все команды `stty -F /dev/ttyHS0 ...` возвращали rc=0, однако effective `stty -a` снова показывал IUCLC/ISTRIP/IXON/ICRNL и другие vendor-default флаги. Это означает, что драйвер восстанавливает termios при каждом новом открытии устройства.

Выпущена 1.3.5:
- UART0 открывается один раз внутри долгоживущего shell bridge;
- termios настраивается на **том же fd**, который затем используется для чтения/записи;
- устранён ранний 0x42 при AUTO reconnect;
- последовательность опыта теперь централизована: mapping -> пауза -> 0x42.

Прогресс остаётся **10/11 — 91%** до первого живого 0x42.


## Версия 1.3.5

На основании отчёта 1.3.4 path-based `stty -F` окончательно заменён на same-FD bridge: один shell-процесс владеет `/dev/ttyHS0`, настраивает именно этот открытый дескриптор и через него же выполняет весь двунаправленный обмен.

Также устранена гонка, позволявшая AUTO преждевременно отправить `0x42` до контрольного mapping.

GitHub Actions run `35917083657` — **success**: автономные тесты, Android-сборка и APK-артефакт прошли.

Прогресс: **10/11 — 91%**.


## Отчёт 1.3.5: переход на нативный UART

Реальный отчёт однозначно показал:
- shell bridge запускался;
- фактический UART0 TX оставался **0**;
- записи завершались `EPIPE`;
- RX оставался 0;
- termios не был бинарно чистым.

Следовательно, дальнейшее наращивание shell/stty прекращено.

Версия 1.4.0:
- открывает `/dev/ttyHS0` нативно;
- применяет `tcsetattr` к тому же fd;
- выставляет 115200 8N1;
- полностью обнуляет input/output/local transformations;
- читает и пишет через тот же fd;
- сохраняет в отчёт фактические termios-маски и признак `binaryClean`.

Прогресс остаётся **10/11 — 91%** до аппаратного подтверждения живого `0x42`.


## Локальная NDK-среда

На рабочей Windows-машине уже установлены NDK `27.0.12077973` и `28.2.13676358`.

Для версии 1.4.0 проект переведён на **NDK 28.2.13676358**. Это устраняет лишнюю установку NDK 26.3 и использует уже имеющийся комплект. Резервный вызов `sdkmanager` также исправлен: теперь ему явно передаётся `--sdk_root`.


## Локальная сборка native UART: обход ndk-build

NDK 28.2 на рабочей машине найден и исправен. Сбой произошёл на этапе `ndk-build`: существующий `Android.mk` в каталоге проекта с пробелами и `!` был объявлен `unknown file`.

Чтобы не переносить рабочий проект и не зависеть от ограничений GNU Make-пути, native UART теперь собирается напрямую NDK Clang. Это отдельная сборочная поправка; исследовательская логика C/D не менялась.

Прогресс исследования остаётся **10/11 — 91%**.


## Прямая сборка JNI через Clang

Локальный сбой `ndk-build` был связан не с исходным C-кодом, а с обработкой абсолютного Windows-пути проекта при передаче `APP_BUILD_SCRIPT`.

Связка `ndk-build/GNU Make` исключена. Нативная библиотека теперь собирается напрямую NDK Clang и передаётся Gradle как готовая JNI-библиотека.

GitHub Actions run `35946294849` — **success**:
- прямой native build через Clang прошёл;
- AArch64 ELF проверен;
- все host-tests прошли;
- Android APK собран;
- артефакт опубликован.

Версия 1.4.0 готова к локальной пересборке и следующему аппаратному опыту.


## Финальное аппаратное подтверждение 1.4.0

Два независимых аппаратных сеанса 1.4.0 завершили исследование.

Зафиксировано:
- native UART0: `binaryClean=true`, 115200, iflag/oflag/lflag = 0;
- реальный mapping: `CH10=C`, `CH11=D`;
- CH10: 1050 отпущено, 1950 нажато;
- CH11: 1050 отпущено, 1950 нажато;
- первый сеанс: 237 живых channel frames, 8 C + 8 D;
- второй сеанс: 154 живых channel frames, 6 C + 6 D;
- всего: 391 channel frames и 28 отмеченных действий;
- каждое C изменяло только changeCount CH10 (+2);
- каждое D изменяло только changeCount CH11 (+2);
- несоответствий не обнаружено.

**Исследовательская цель достигнута: 11/11 — 100%.**

Итоговая спецификация для будущего отдельного приложения:
`docs/MK15_CD_FINAL_SPEC.md`.

## SDK для разработчиков

После завершения исследования результат закреплён как подключаемая Android-библиотека `mk15-sdk` версии **1.0.0**.

Артефакт: `out/MK15-CD-SDK-1.0.0.aar`.

Библиотека содержит:
- нативный `/dev/ttyHS0`, 115200 8N1;
- runtime mapping `0x48`; 
- поток `0x42`, 4 Гц;
- события C/D;
- кодовые аппаратные свидетельства;
- SHA-256 исходных отчётов.

GitHub Actions run `36052471874` — success.

Исследовательская цель остаётся завершённой: **11/11 — 100%**. SDK является формализованной передачей результата будущим разработчикам.
